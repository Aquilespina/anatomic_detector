name: anatomic_detector
description: "App para detección de puntos anatómicos"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_pose_detection: ^0.14.0
  image_picker: ^1.0.4
  camera: ^0.10.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true